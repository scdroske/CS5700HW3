package Shapes;

public class EmbeddedPictureIntrinsic {
//read in file/stream of pictures
//check if file/stream already exists in dictionary
//if so, start next method, else add it to the dictionary
//if in dictionary, use its
//send info to emb pic factory or w/e to then add the extrinsic state info (decorate)
}
